<html>
<head>
  {{> header}}
</head>
<body>
<div class="container">
  <h1>Confirm Deletion</h1>
  <p>You are about to delete the course: <strong>{{course.name}}</strong>.</p>
  <p>Please type the word <strong>delete</strong> below to confirm.</p>
  <form action="/deleteCourse/{{course._id}}" method="POST">
    <div class="form-group">
      <input type="text" name="confirmation" class="form-control" placeholder="Type 'delete' here" required>
    </div>
    <button type="submit" class="btn btn-danger">Confirm Deletion</button>
  </form>
  <a href="/courses" class="btn btn-secondary mt-3">Cancel</a>
</div>
</body>
</html>
